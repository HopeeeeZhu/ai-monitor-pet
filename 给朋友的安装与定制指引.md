# 小安竺来咯 — 安装与定制指引（发给朋友版）

一个 macOS 桌面宠物，监测你电脑上 AI 工具的工作状态：AI 在跑任务时宝宝跑步，需要授权时举手提醒你，任务跑完跳跃庆祝，没事时睡觉。宠物头顶气泡会显示「工具 · 项目 · 任务」。

---

## 一、安装（5 分钟）

1. 到 [Releases 页面](https://github.com/HopeeeeZhu/ai-monitor-pet/releases) 下载最新版本的 dmg，双击打开，把 `小安竺来咯.app` 拖进 Applications 文件夹。
2. 第一次打开：在「应用程序」里**右键**点 小安竺来咯 → 选「打开」→ 弹窗里再点「打开」。
   - 如果提示「已损坏，无法打开」：打开「终端」App，粘贴下面这行后按回车，再重新打开：
     ```
     xattr -cr /Applications/小安竺来咯.app
     ```
3. 授予辅助功能权限（用于检测「需要授权」弹窗）：
   系统设置 → 隐私与安全性 → 辅助功能 → 打开 小安竺来咯 的开关。
4. 宠物出现在屏幕上后：左键拖动换位置，左键单击展开状态面板，**右键**可以调宠物大小或退出。

---

## 二、换成自己小朋友的照片（约 30 分钟）

宠物形象就是 16 张 PNG 图片，替换它们即可。

### 第 1 步：用 AI 生成 16 张图

用任意 AI 绘图工具（豆包、即梦、ChatGPT、Midjourney 都行），上传小朋友的照片，生成 **4 个动作 × 每个动作 4 帧**：

| 动作 | 文件名 | 画面 |
|------|--------|------|
| 睡觉 | sleep_0.png ~ sleep_3.png | 趴着/躺着睡，4 帧间微小差异（呼吸起伏） |
| 跑步 | run_0.png ~ run_3.png | 跑动，4 帧是不同的跑步姿势 |
| 举手 | wave_0.png ~ wave_3.png | 举手挥手，4 帧是挥动的不同位置 |
| 庆祝 | celebrate_0.png ~ celebrate_3.png | 跳跃欢呼，4 帧是跳起的不同高度 |

参考提示词：

> 把照片里的小朋友画成 Q 版像素风/卡通风全身立绘，透明背景，[睡觉/跑步/举手挥手/跳跃庆祝] 姿势，生成 4 帧连贯的动画帧

要求：**PNG 格式、透明背景**、建议高度 512px 以上、人物大致居中。
偷懒办法：每个动作只生成 1 张，复制 4 份改名——动画会变静止画，但完全能用。

### 第 2 步：替换图片

1. 打开「应用程序」文件夹，右键 小安竺来咯 → 「显示包内容」。
2. 进入 `Contents/Resources/`，把你的 16 张图拖进去**覆盖同名文件**（文件名必须一字不差）。

### 第 3 步：重新签名并重启

改过 App 内容后必须重新签名，否则打不开。打开「终端」，粘贴这行按回车：

```
codesign --force --deep --sign - /Applications/小安竺来咯.app
```

然后右键宠物退出（如果在运行），重新打开 App。
注意：重签后系统可能把它当新 App，辅助功能权限需要去系统设置里**重新勾一次**。

---

## 三、AI 工具适配情况

### 装好就能用（零配置）

| AI 工具 | 检测方式 | 能看到什么 |
|---------|---------|-----------|
| **Claude 桌面端**（含 Cowork） | 自动监测 /Applications/Claude.app + 会话日志 | 状态 + 当前任务名 |
| **Codex 桌面端** | 自动监测 /Applications/Codex.app + 会话日志 | 状态 + 项目名 + 任务名 |
| **Aone Copilot**（IntelliJ 内） | 自动监听 ~/.r2c 日志 | 状态 + 项目 |

只要这些 App 装在 /Applications 标准位置，打开宠物就开始工作。

### 需要简单配置

**Qoder**：在 `~/.qoder/settings.json` 里配 hooks，把事件写到 `~/.ai_monitor/events.jsonl`，每行一条 JSON：

```json
{"tool": "qoder", "event": "started", "session_id": "xxx", "cwd": "/path/to/project"}
```

event 取值：`started`（开始跑）/ `waiting`（等授权）/ `completed`（跑完）。

### 暂不支持（需要改代码）

Cursor、Windsurf、Gemini CLI、ChatGPT 桌面端等其他工具目前没有适配。如果你常用的工具不在上面的列表里，把工具名字告诉 hopeeee——大部分工具只要有 hook 机制或本地日志，加一个适配器就能接进来。

---

## 常见问题

- **宠物一直睡觉不动**：确认你的 AI 工具在支持列表里、且装在 /Applications；Claude/Codex 跑任务约 2 秒内宠物会跑起来。
- **「需要授权」不提醒**：检查辅助功能权限（重签名后要重新授权）。
- **想调大小**：右键宠物 → 宠物大小。
- **气泡挡视线**：左键把宠物拖到屏幕角落。
